---
title: Stagger Fade List
description: List component with staggered fade-in animation.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Preview } from '@/components/preview';
import { StaggerFadeList } from '@/registry/new-york/animations/lists/stagger-fade';
import { Music2, Clock } from "lucide-react";

## Overview

The `StaggerFadeList` component creates a sequential fade-in effect for list items. Perfect for subtle, elegant content reveals.

## Preview

<Preview
  background="default"
  code={`import { StaggerFadeList } from "@/components/animations/lists/stagger-fade"
import { Music2, Clock } from "lucide-react"

const playlist = [
  { title: "Midnight City", artist: "M83", duration: "4:03" },
  { title: "Starboy", artist: "The Weeknd", duration: "3:50" },
  { title: "Get Lucky", artist: "Daft Punk", duration: "6:09" },
  { title: "The Less I Know The Better", artist: "Tame Impala", duration: "3:36" }
]

<StaggerFadeList staggerDelay={0.1} className="w-full max-w-md space-y-1">
  {playlist.map((song, i) => (
    <div key={i} className="group flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-accent transition-colors cursor-pointer">
      <div className="flex h-10 w-10 items-center justify-center rounded-md border bg-background">
        <Music2 className="h-4 w-4 text-muted-foreground" />
      </div>
      <div className="flex-1 min-w-0">
        <p className="text-sm font-medium leading-none mb-1">{song.title}</p>
        <p className="text-sm text-muted-foreground">{song.artist}</p>
      </div>
      <div className="flex items-center gap-1.5 text-muted-foreground">
        <Clock className="h-3.5 w-3.5" />
        <span className="text-xs font-mono tabular-nums">{song.duration}</span>
      </div>
    </div>
  ))}
</StaggerFadeList>`}
>
  <StaggerFadeList staggerDelay={0.1} className="w-full max-w-md space-y-1">
    {[
      { title: "Midnight City", artist: "M83", duration: "4:03" },
      { title: "Starboy", artist: "The Weeknd", duration: "3:50" },
      { title: "Get Lucky", artist: "Daft Punk", duration: "6:09" },
      { title: "The Less I Know The Better", artist: "Tame Impala", duration: "3:36" }
    ].map((song, i) => (
      <div key={i} className="group flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-accent transition-colors cursor-pointer">
        <div className="flex h-10 w-10 items-center justify-center rounded-md border bg-background">
          <Music2 className="h-4 w-4 text-muted-foreground" />
        </div>
        <div className="flex-1 min-w-0">
          <p className="text-sm font-medium leading-none mb-1">{song.title}</p>
          <p className="text-sm text-muted-foreground">{song.artist}</p>
        </div>
        <div className="flex items-center gap-1.5 text-muted-foreground">
          <Clock className="h-3.5 w-3.5" />
          <span className="text-xs font-mono tabular-nums">{song.duration}</span>
        </div>
      </div>
    ))}
  </StaggerFadeList>
</Preview>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn add https://animbits.dev/r/lists-stagger-fade.json
    ```
  </Tab>
  <Tab value="Manual">
    **Step 1: Add the utility**

    ```tsx title="lib/utils.ts"
    import { clsx, type ClassValue } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    **Step 2: Copy the component**

    ```tsx title="components/animations/lists/stagger-fade.tsx"
"use client";
import * as React from "react";
import { motion, HTMLMotionProps } from "motion/react";
import { cn } from "@/lib/utils";
export interface StaggerFadeListProps
  extends Omit<HTMLMotionProps<"div">, "children"> {
  children: React.ReactNode[];
  staggerDelay?: number;
  duration?: number;
  from?: number;
  as?: "div" | "ul" | "ol" | "section" | "article";
}
export function StaggerFadeList({
  children,
  staggerDelay = 0.1,
  duration = 0.4,
  from = 0,
  as: Component = "div",
  className,
  ...props
}: StaggerFadeListProps) {
  const childrenArray = React.Children.toArray(children);
  const MotionComponent = motion[Component as keyof typeof motion] as any;
  return (
    <MotionComponent className={cn(className)} {...props}>
      {childrenArray.map((child, index) => (
        <motion.div
          key={index}
          initial={{ opacity: from }}
          animate={{ opacity: 1 }}
          transition={{
            duration,
            delay: index * staggerDelay,
            ease: "easeOut",
          }}
        >
          {child}
        </motion.div>
      ))}
    </MotionComponent>
  );
}

```
  </Tab>
</Tabs>

## Usage

```tsx
import { StaggerFadeList } from "@/components/animations/lists/stagger-fade"

const items = ["Item 1", "Item 2", "Item 3"]

export default function Example() {
  return (
    <StaggerFadeList staggerDelay={0.1}>
      {items.map((item, index) => (
        <div key={index} className="p-4 border rounded-lg">
          {item}
        </div>
      ))}
    </StaggerFadeList>
  )
}
```

## Examples

### Default Fade

<Preview
  background="default"
  code={`<StaggerFadeList>
  {items.map((item, i) => <div key={i}>{item}</div>)}
</StaggerFadeList>`}
>
  <StaggerFadeList className="w-full max-w-md space-y-2">
    {["Feature One", "Feature Two", "Feature Three"].map((item, i) => (
      <div key={i} className="flex items-center gap-3 rounded-lg border bg-card p-4 text-card-foreground shadow-sm">
        <div className="flex h-8 w-8 items-center justify-center rounded-md bg-primary/10 text-primary font-semibold text-sm">
          {i + 1}
        </div>
        <p className="text-sm font-medium">{item}</p>
      </div>
    ))}
  </StaggerFadeList>
</Preview>

### Fast Stagger

<Preview
  background="default"
  code={`<StaggerFadeList staggerDelay={0.05} duration={0.2}>
  {items.map((item, i) => <div key={i}>{item}</div>)}
</StaggerFadeList>`}
>
  <StaggerFadeList staggerDelay={0.05} duration={0.2} className="w-full max-w-md space-y-1.5">
    {["Quick One", "Quick Two", "Quick Three", "Quick Four"].map((item, i) => (
      <div key={i} className="rounded-md border bg-muted/50 px-4 py-2.5 text-sm hover:bg-muted transition-colors">
        {item}
      </div>
    ))}
  </StaggerFadeList>
</Preview>

### Slow Reveal

<Preview
  background="default"
  code={`<StaggerFadeList staggerDelay={0.2} duration={0.6}>
  {items.map((item, i) => <div key={i}>{item}</div>)}
</StaggerFadeList>`}
>
  <StaggerFadeList staggerDelay={0.2} duration={0.6} className="w-full max-w-md space-y-3">
    {["Slow One", "Slow Two", "Slow Three"].map((item, i) => (
      <div key={i} className="rounded-lg border-2 bg-card px-6 py-4 text-center font-medium shadow-sm">
        {item}
      </div>
    ))}
  </StaggerFadeList>
</Preview>

### Grid Layout

<Preview
  background="default"
  code={`<StaggerFadeList as="div" className="grid grid-cols-3 gap-4">
  {items.map((item, i) => <div key={i}>{item}</div>)}
</StaggerFadeList>`}
>
  <StaggerFadeList as="div" className="grid grid-cols-3 gap-3 w-full max-w-md">
    {["Design", "Build", "Ship", "Test", "Deploy", "Scale"].map((item, i) => (
      <div key={i} className="group flex aspect-square flex-col items-center justify-center gap-2 rounded-lg border bg-card p-4 shadow-sm hover:shadow-md hover:border-primary/50 transition-all cursor-pointer">
        <span className="text-3xl font-bold text-primary">{i + 1}</span>
        <span className="text-sm font-medium text-center">{item}</span>
      </div>
    ))}
  </StaggerFadeList>
</Preview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode[]` | - | Array of items to animate |
| `staggerDelay` | `number` | `0.1` | Delay between each item in seconds |
| `duration` | `number` | `0.4` | Animation duration in seconds |
| `from` | `number` | `0` | Initial opacity value |
| `as` | `keyof JSX.IntrinsicElements` | `"div"` | Container element type |
| `className` | `string` | - | Additional CSS classes |
