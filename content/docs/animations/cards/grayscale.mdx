---
title: Grayscale Card
description: Card with grayscale to color transition on hover.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Preview, PreviewGrid } from '@/components/preview';
import { CardGrayscale } from '@/registry/new-york/animations/cards/grayscale';

## Overview

The `CardGrayscale` creates a smooth grayscale to color transition effect on hover. Perfect for image galleries, portfolios, and visual showcases.

## Preview

<Preview
  background="default"
  code={`import { CardGrayscale } from "@/components/animations/cards/grayscale"
import { ArrowRight } from "lucide-react"

<div className="relative w-full max-w-2xl min-h-[320px] rounded-2xl overflow-hidden flex shadow-xl bg-white dark:bg-neutral-950 border border-neutral-200 dark:border-neutral-800">
  {/* Left side - Image with grayscale effect */}
  <CardGrayscale className="w-1/2 shrink-0">
    <img 
      src="https://images.unsplash.com/photo-1523712999610-f77fbcfc3843?q=80&w=1000&auto=format&fit=crop" 
      alt="Forest Path" 
      className="absolute inset-0 w-full h-full object-cover" 
    />
  </CardGrayscale>
  
  {/* Right side - Content */}
  <div className="w-1/2 p-8 flex flex-col justify-center">
    <div className="uppercase tracking-wide text-sm text-indigo-500 font-semibold mb-1">Explore Nature</div>
    <h2 className="text-3xl font-bold mb-3 text-neutral-900 dark:text-white leading-tight">Into the Wild</h2>
    <div className="text-neutral-600 dark:text-neutral-400 mb-6 leading-relaxed">
      Discover the hidden paths of the ancient forest. Hover over the image to bring the vibrant colors of nature to life.
    </div>
    <button className="group inline-flex h-10 w-fit items-center gap-2 rounded-full bg-neutral-900 dark:bg-white px-6 text-sm font-medium text-white dark:text-neutral-900 hover:bg-neutral-800 dark:hover:bg-neutral-100 transition-all">
      Start Journey
      <ArrowRight className="w-4 h-4 transition-transform group-hover:translate-x-1" />
    </button>
  </div>
</div>`}
>
  <div className="relative w-full max-w-2xl min-h-[320px] rounded-2xl overflow-hidden flex shadow-xl bg-white dark:bg-neutral-950 border border-neutral-200 dark:border-neutral-800">
    {/* Left side - Image with grayscale effect */}
    <CardGrayscale className="w-1/2 shrink-0">
      <img 
        src="https://images.unsplash.com/photo-1523712999610-f77fbcfc3843?q=80&w=1000&auto=format&fit=crop" 
        alt="Forest Path" 
        className="absolute inset-0 w-full h-full object-cover" 
      />
    </CardGrayscale>
    
    {/* Right side - Content */}
    <div className="w-1/2 p-8 flex flex-col justify-center">
      <div className="uppercase tracking-wide text-sm text-indigo-500 font-semibold mb-1">Explore Nature</div>
      <h2 className="text-3xl font-bold mb-3 text-neutral-900 dark:text-white leading-tight">Into the Wild</h2>
      <div className="text-neutral-600 dark:text-neutral-400 mb-6 leading-relaxed">
        Discover the hidden paths of the ancient forest. Hover over the image to bring the vibrant colors of nature to life.
      </div>
      <button className="group inline-flex h-10 w-fit items-center gap-2 rounded-full bg-neutral-900 dark:bg-white px-6 text-sm font-medium text-white dark:text-neutral-900 hover:bg-neutral-800 dark:hover:bg-neutral-100 transition-all">
        Start Journey
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="w-4 h-4 transition-transform group-hover:translate-x-1"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
      </button>
    </div>
  </div>
</Preview>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn add https://animbits.dev/r/cards-grayscale.json
    ```
  </Tab>
  <Tab value="Manual">
    **Step 1: Add the utility**

    ```tsx title="lib/utils.ts"
    import { clsx, type ClassValue } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    **Step 2: Copy the component**

    ```tsx title="components/animations/cards/grayscale.tsx"
    "use client"
    
    import * as React from "react"
    import * as motion from "framer-motion/client"
    import type { HTMLMotionProps } from "framer-motion"
    import { cn } from "@/lib/utils"
    
    export interface CardGrayscaleProps extends Omit<HTMLMotionProps<"div">, "children"> {
      children: React.ReactNode
      duration?: number
    }
    
    export function CardGrayscale({
      children,
      className,
      duration = 0.5,
      ...props
    }: CardGrayscaleProps) {
      const [isHovered, setIsHovered] = React.useState(false)
    
      return (
        <motion.div
          className={cn("relative overflow-hidden", className)}
          onHoverStart={() => setIsHovered(true)}
          onHoverEnd={() => setIsHovered(false)}
          animate={{
            filter: isHovered ? "grayscale(0%)" : "grayscale(100%)"
          }}
          transition={{ duration }}
          {...props}
        >
          {children}
        </motion.div>
      )
    }
    ```
  </Tab>
</Tabs>

## Usage

```tsx
import { CardGrayscale } from "@/components/animations/cards/grayscale"

<CardGrayscale className="rounded-lg overflow-hidden">
  <img src="/photo.jpg" alt="Photo" className="w-full h-full object-cover" />
</CardGrayscale>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `duration` | `number` | `0.5` | Animation duration in seconds |
| `className` | `string` | `undefined` | Additional CSS classes |
