---
title: Text Highlight
description: A smooth, underline highlight animation for text.
component: true
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { Preview } from '@/components/preview';
import TextHighlightDemo from '@/registry/new-york/animations/text/text-highlight-demo';
import { TextHighlight } from '@/registry/new-york/animations/text/text-highlight';

<Preview code={`"use client";

import { TextHighlight } from "./text-highlight";

export default function TextHighlightDemo() {
    return (
        <div className="flex flex-col items-center justify-center gap-8 p-8 text-center">
            <h2 className="text-3xl font-bold md:text-4xl max-w-2xl leading-relaxed">
                Make your text <br />
                <TextHighlight className="mx-2" delay={0} color="#fbbf24">
                    stand out
                </TextHighlight>
                with a <br />
                <TextHighlight color="#2dd4bf" delay={0.3} duration={0.8}>
                    smooth underline
                </TextHighlight>
                effect.
            </h2>
            
            <p className="text-muted-foreground text-lg max-w-lg mt-4">
                It works perfectly for <TextHighlight color="#60a5fa" delay={0.6}>highlighting keywords</TextHighlight> in your paragraphs too.
            </p>
        </div>
    );
}`}>
  <TextHighlightDemo />
</Preview>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn add https://animbits.dev/r/text-highlight.json
    ```
  </Tab>
  <Tab value="Manual">
    <Steps>
      <Step>
        **Install dependencies**
        ```bash
        npm install motion
        ```
      </Step>
      <Step>
        **Copy the component code**
        ```tsx title="components/animations/text/text-highlight.tsx"
        "use client";

        import { cn } from "@/lib/utils";
        import { motion } from "motion/react";

        interface TextHighlightProps {
            children: React.ReactNode;
            className?: string;
            delay?: number;
            duration?: number;
            color?: string;
        }

        export function TextHighlight({
            children,
            className,
            delay = 0,
            duration = 1.2,
            color = "#fde047",
        }: TextHighlightProps) {
            return (
                <span className={cn("relative inline-block px-2 py-1", className)}>
                    {/* Strong background glow */}
                    <motion.span
                        className="absolute inset-0 rounded-xl blur-2xl -z-20"
                        initial={{ opacity: 0, scale: 0.5 }}
                        animate={{ opacity: 0.6, scale: 1.3 }}
                        transition={{
                            duration: duration * 0.6,
                            delay,
                            ease: [0.16, 1, 0.3, 1],
                        }}
                        style={{
                            backgroundColor: color,
                        }}
                    />

                    {/* Main background highlight */}
                    <motion.span
                        className="absolute inset-0 rounded-lg -z-10"
                        initial={{ opacity: 0, scaleX: 0 }}
                        animate={{ opacity: 0.5, scaleX: 1 }}
                        transition={{
                            duration: duration * 0.7,
                            delay: delay + 0.1,
                            ease: [0.16, 1, 0.3, 1],
                        }}
                        style={{
                            backgroundColor: color,
                            transformOrigin: "left center",
                        }}
                    />

                    {/* Thick underline */}
                    <motion.span
                        className="absolute bottom-1 left-0 h-[3px] rounded-full"
                        initial={{ width: 0, opacity: 0 }}
                        animate={{ width: "100%", opacity: 1 }}
                        transition={{
                            duration: duration * 0.5,
                            delay: delay + 0.4,
                            ease: [0.16, 1, 0.3, 1],
                        }}
                        style={{
                            backgroundColor: color,
                        }}
                    />

                    {/* Shimmer sweep - very visible */}
                    <motion.span
                        className="absolute inset-0 rounded-lg -z-5"
                        initial={{
                            x: "-100%",
                            opacity: 0
                        }}
                        animate={{
                            x: "200%",
                            opacity: [0, 1, 0]
                        }}
                        transition={{
                            duration: duration * 0.8,
                            delay: delay + 0.2,
                            ease: [0.22, 1, 0.36, 1],
                        }}
                        style={{
                            background: `linear-gradient(90deg, transparent 0%, ${color} 50%, transparent 100%)`,
                            opacity: 0.6,
                        }}
                    />

                    {/* Text with brightness boost */}
                    <motion.span
                        className="relative font-medium"
                        initial={{ y: 2, opacity: 0.7 }}
                        animate={{ y: 0, opacity: 1 }}
                        transition={{
                            duration: duration * 0.4,
                            delay: delay + 0.05,
                            ease: [0.16, 1, 0.3, 1],
                        }}
                        style={{
                            filter: "brightness(1.1)",
                        }}
                    >
                        {children}
                    </motion.span>
                </span>
            );
        }
        ```
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Examples

### Default Highlight

<Preview code={`import { TextHighlight } from "./text-highlight";

export default function Example() {
  return (
    <div className="text-lg">
      This is a <TextHighlight>default highlight</TextHighlight> with standard styling
    </div>
  );
}`}>
  <div className="p-8">
    <div className="text-lg">
      This is a <TextHighlight>default highlight</TextHighlight> with standard styling
    </div>
  </div>
</Preview>

### Different Colors

<Preview code={`import { TextHighlight } from "./text-highlight";

export default function Example() {
  return (
    <div className="text-lg">
      <TextHighlight color="#60a5fa" delay={0}>Blue</TextHighlight>,{" "}
      <TextHighlight color="#34d399" delay={0.2}>Green</TextHighlight>,{" "}
      <TextHighlight color="#f472b6" delay={0.4}>Pink</TextHighlight>, and{" "}
      <TextHighlight color="#a78bfa" delay={0.6}>Purple</TextHighlight>
    </div>
  );
}`}>
  <div className="p-8">
    <div className="text-lg">
      <TextHighlight color="#60a5fa" delay={0}>Blue</TextHighlight>,{" "}
      <TextHighlight color="#34d399" delay={0.2}>Green</TextHighlight>,{" "}
      <TextHighlight color="#f472b6" delay={0.4}>Pink</TextHighlight>, and{" "}
      <TextHighlight color="#a78bfa" delay={0.6}>Purple</TextHighlight>
    </div>
  </div>
</Preview>

### Sequential Animation

<Preview code={`import { TextHighlight } from "./text-highlight";

export default function Example() {
  return (
    <h2 className="text-2xl font-bold">
      Make your <TextHighlight delay={0} color="#fbbf24">important text</TextHighlight> stand out with{" "}
      <TextHighlight delay={0.5} color="#2dd4bf">beautiful animations</TextHighlight>
    </h2>
  );
}`}>
  <div className="p-8">
    <h2 className="text-2xl font-bold">
      Make your <TextHighlight delay={0} color="#fbbf24">important text</TextHighlight> stand out with{" "}
      <TextHighlight delay={0.5} color="#2dd4bf">beautiful animations</TextHighlight>
    </h2>
  </div>
</Preview>

### Animation Speed

<Preview code={`import { TextHighlight } from "./text-highlight";

export default function Example() {
  return (
    <div className="flex flex-col gap-4">
      <div className="text-lg">
        This has a <TextHighlight duration={0.8} color="#ef4444">quick highlight</TextHighlight> effect
      </div>
      <div className="text-lg">
        This has a <TextHighlight duration={2.5} color="#8b5cf6">slow, dramatic</TextHighlight> effect
      </div>
    </div>
  );
}`}>
  <div className="p-8 flex flex-col gap-4">
    <div className="text-lg">
      This has a <TextHighlight duration={0.8} color="#ef4444">quick highlight</TextHighlight> effect
    </div>
    <div className="text-lg">
      This has a <TextHighlight duration={2.5} color="#8b5cf6">slow, dramatic</TextHighlight> effect
    </div>
  </div>
</Preview>

## Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `children` | `React.ReactNode` | - | The text content to highlight. |
| `className` | `string` | - | Additional CSS classes. |
| `delay` | `number` | `0` | Delay before animation starts (in seconds). |
| `duration` | `number` | `1.2` | Duration of the animation (in seconds). |
| `color` | `string` | `"#fde047"` | The color of the highlight - used for background, underline, glow, and shimmer effects. |

## Features

- **Strong background glow**: 60% opacity blur-2xl halo that extends beyond the text
- **Colored background**: 50% opacity background that sweeps from left to right
- **Thick underline**: 3px underline that draws across the text
- **Shimmer effect**: Visible flash of light that sweeps across (fades in and out)
- **Text enhancement**: Font weight medium with 10% brightness boost
- **Smooth animations**: Custom easing for professional motion
- **Fully customizable**: Adjust color, duration, and delay
