---
title: Wiggle Icon
description: One-shot wiggle animation (rotate left-right) for icons.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Preview } from '@/components/preview';
import { WiggleIcon } from '@/registry/new-york/animations/icons/wiggle';
import { Bell, BellRing, Phone } from 'lucide-react';

## Overview

The `WiggleIcon` component creates a rotational wiggle animation. Perfect for notifications, bells, and playful interactions.

## Preview

<Preview
  background="grid"
  code={`import { WiggleIcon } from "@/components/animations/icons/wiggle"
import { Bell } from "lucide-react"

<WiggleIcon trigger="hover" intensity={10}>
  <Bell className="w-8 h-8 text-yellow-500" />
</WiggleIcon>`}
>
  <div className="flex gap-8 items-center justify-center">
    <WiggleIcon trigger="hover" intensity={10} className="cursor-pointer">
      <Bell className="w-8 h-8 text-yellow-500" />
    </WiggleIcon>
    <WiggleIcon trigger="tap" intensity={15}>
      <BellRing className="w-8 h-8 text-blue-500" />
    </WiggleIcon>
    <WiggleIcon trigger="mount" intensity={8}>
      <Phone className="w-8 h-8 text-green-500" />
    </WiggleIcon>
  </div>
</Preview>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn add https://animbits.dev/r/animations/icons/wiggle
    ```
  </Tab>
  <Tab value="Manual">
    ```tsx title="components/animations/icons/wiggle.tsx"
    "use client"

    import { motion, type HTMLMotionProps } from "framer-motion"
    import { wiggleVariants } from "@/lib/animation-presets"
    import { useState } from "react"

    interface WiggleIconProps extends Omit<HTMLMotionProps<"div">, "animate"> {
      children: React.ReactNode
      trigger?: "hover" | "tap" | "mount"
      intensity?: number
    }

    export function WiggleIcon({
      children,
      trigger = "hover",
      intensity = 10,
      ...props
    }: WiggleIconProps) {
      const [key, setKey] = useState(0)

      const customVariants = {
        initial: { rotate: 0 },
        animate: {
          rotate: [0, -intensity, intensity, -intensity, intensity, 0],
        },
      }

      const handleTrigger = () => {
        setKey((prev) => prev + 1)
      }

      return (
        <motion.div
          key={key}
          variants={customVariants}
          initial="initial"
          animate={trigger === "mount" ? "animate" : undefined}
          whileHover={trigger === "hover" ? "animate" : undefined}
          whileTap={trigger === "tap" ? "animate" : undefined}
          onHoverStart={trigger === "hover" ? handleTrigger : undefined}
          onTap={trigger === "tap" ? handleTrigger : undefined}
          transition={{ duration: 0.5, ease: "easeInOut" }}
          {...props}
        >
          {children}
        </motion.div>
      )
    }
    ```

    **Dependencies:**
    - `wiggleVariants` from `@/lib/animation-presets`
  </Tab>
</Tabs>

## Usage

```tsx
import { WiggleIcon } from "@/components/animations/icons/wiggle"
import { Bell } from "lucide-react"

export default function Example() {
  return (
    <WiggleIcon trigger="hover" intensity={10}>
      <Bell className="w-6 h-6" />
    </WiggleIcon>
  )
}
```

## Examples

### Hover Trigger

<Preview
  background="dots"
  code={`<WiggleIcon trigger="hover" intensity={10} className="cursor-pointer">
  <Bell className="w-12 h-12 text-yellow-500" />
</WiggleIcon>`}
>
  <WiggleIcon trigger="hover" intensity={10} className="cursor-pointer">
    <Bell className="w-12 h-12 text-yellow-500" />
  </WiggleIcon>
</Preview>

### Tap Trigger

<Preview
  background="dots"
  code={`<WiggleIcon trigger="tap" intensity={15}>
  <BellRing className="w-12 h-12 text-blue-500" />
</WiggleIcon>`}
>
  <WiggleIcon trigger="tap" intensity={15}>
    <BellRing className="w-12 h-12 text-blue-500" />
  </WiggleIcon>
</Preview>

### Subtle Wiggle

<Preview
  background="dots"
  code={`<WiggleIcon trigger="hover" intensity={5} className="cursor-pointer">
  <Phone className="w-12 h-12 text-green-500" />
</WiggleIcon>`}
>
  <WiggleIcon trigger="hover" intensity={5} className="cursor-pointer">
    <Phone className="w-12 h-12 text-green-500" />
  </WiggleIcon>
</Preview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The icon element to animate |
| `trigger` | `"hover"` \| `"tap"` \| `"mount"` | `"hover"` | When to trigger the wiggle animation |
| `intensity` | `number` | `10` | Wiggle intensity (rotation angle in degrees) |
