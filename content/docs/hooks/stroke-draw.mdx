---
title: useStrokeDraw
description: SVG path drawing animation that reveals strokes progressively.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## Overview

The `useStrokeDraw` hook animates SVG paths with a drawing effect, perfect for logos, icons, signatures, or decorative elements.

<Callout type="info">
  **SVG Only:** This hook is specifically designed for SVG `<path>` elements using Framer Motion.
</Callout>

## Installation

<Tabs items={['Manual', 'CLI']}>
  <Tab value="Manual">
    ```tsx title="lib/hooks/use-stroke-draw.ts"
    import { type MotionProps } from "framer-motion"

    export interface UseStrokeDrawOptions {
      duration?: number
      delay?: number
      repeat?: boolean
      ease?: string | number[]
    }

    export function useStrokeDraw(options: UseStrokeDrawOptions = {}): MotionProps {
      const {
        duration = 1.5,
        delay = 0,
        repeat = false,
        ease = "easeInOut",
      } = options

      return {
        initial: { pathLength: 0, opacity: 0 },
        animate: { pathLength: 1, opacity: 1 },
        transition: {
          pathLength: { duration, delay, ease },
          opacity: { duration: 0.2, delay },
          repeat: repeat ? Infinity : 0,
          repeatDelay: 1,
        },
      }
    }
    ```
  </Tab>
  <Tab value="CLI">
    ```bash
    npx shadcn add https://animbits.dev/r/hooks-use-stroke-draw.json
    ```
  </Tab>
</Tabs>

## Usage

### Basic Example

```tsx
import { motion } from "framer-motion"
import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

export function DrawingLogo() {
  const strokeProps = useStrokeDraw()
  
  return (
    <svg width="200" height="200" viewBox="0 0 200 200">
      <motion.path
        d="M 50 100 Q 100 50 150 100 T 250 100"
        stroke="currentColor"
        strokeWidth="3"
        fill="none"
        {...strokeProps}
      />
    </svg>
  )
}
```

### With Custom Duration

```tsx
import { motion } from "framer-motion"
import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

export function SlowDraw() {
  const strokeProps = useStrokeDraw({ 
    duration: 3,
    delay: 0.5,
  })
  
  return (
    <svg width="300" height="300" viewBox="0 0 300 300">
      <motion.path
        d="M 150 50 L 250 150 L 150 250 L 50 150 Z"
        stroke="#3b82f6"
        strokeWidth="4"
        fill="none"
        {...strokeProps}
      />
    </svg>
  )
}
```

### Repeating Animation

```tsx
import { motion } from "framer-motion"
import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

export function ContinuousDraw() {
  const strokeProps = useStrokeDraw({ 
    duration: 2,
    repeat: true, // Loops forever
  })
  
  return (
    <svg width="100" height="100" viewBox="0 0 100 100">
      <motion.circle
        cx="50"
        cy="50"
        r="40"
        stroke="currentColor"
        strokeWidth="2"
        fill="none"
        {...strokeProps}
      />
    </svg>
  )
}
```

### Multiple Paths with Stagger

<Tabs items={['Sequential', 'Staggered', 'Simultaneous']}>
  <Tab value="Sequential">
    ```tsx
    import { motion } from "framer-motion"
    import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

    export function SequentialDraw() {
      const path1 = useStrokeDraw({ delay: 0 })
      const path2 = useStrokeDraw({ delay: 1.5 })
      const path3 = useStrokeDraw({ delay: 3 })
      
      return (
        <svg width="300" height="100" viewBox="0 0 300 100">
          <motion.path d="M 10 50 L 90 50" stroke="currentColor" {...path1} />
          <motion.path d="M 110 50 L 190 50" stroke="currentColor" {...path2} />
          <motion.path d="M 210 50 L 290 50" stroke="currentColor" {...path3} />
        </svg>
      )
    }
    ```
  </Tab>
  <Tab value="Staggered">
    ```tsx
    import { motion } from "framer-motion"
    import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

    export function StaggeredDraw() {
      const path1 = useStrokeDraw({ delay: 0 })
      const path2 = useStrokeDraw({ delay: 0.3 })
      const path3 = useStrokeDraw({ delay: 0.6 })
      
      return (
        <svg width="300" height="100" viewBox="0 0 300 100">
          <motion.path d="M..." stroke="currentColor" {...path1} />
          <motion.path d="M..." stroke="currentColor" {...path2} />
          <motion.path d="M..." stroke="currentColor" {...path3} />
        </svg>
      )
    }
    ```
  </Tab>
  <Tab value="Simultaneous">
    ```tsx
    import { motion } from "framer-motion"
    import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

    export function SimultaneousDraw() {
      const strokeProps = useStrokeDraw() // Same props for all
      
      return (
        <svg width="300" height="100" viewBox="0 0 300 100">
          <motion.path d="M..." stroke="currentColor" {...strokeProps} />
          <motion.path d="M..." stroke="currentColor" {...strokeProps} />
          <motion.path d="M..." stroke="currentColor" {...strokeProps} />
        </svg>
      )
    }
    ```
  </Tab>
</Tabs>

## API Reference

### Options

<div className="overflow-x-auto">
  <table className="w-full">
    <thead>
      <tr>
        <th>Option</th>
        <th>Type</th>
        <th>Default</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`duration`</td>
        <td>`number`</td>
        <td>`1.5`</td>
        <td>Animation duration in seconds</td>
      </tr>
      <tr>
        <td>`delay`</td>
        <td>`number`</td>
        <td>`0`</td>
        <td>Delay before animation starts</td>
      </tr>
      <tr>
        <td>`repeat`</td>
        <td>`boolean`</td>
        <td>`false`</td>
        <td>Whether to loop the animation</td>
      </tr>
      <tr>
        <td>`ease`</td>
        <td>`string \| number[]`</td>
        <td>`"easeInOut"`</td>
        <td>Animation easing function</td>
      </tr>
    </tbody>
  </table>
</div>

### Returns

Returns `MotionProps` to spread on `motion.path` or other SVG elements

## Examples

### Logo Reveal

```tsx
import { motion } from "framer-motion"
import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

export function LogoReveal() {
  const strokeProps = useStrokeDraw({ duration: 2 })
  
  return (
    <svg width="200" height="200" viewBox="0 0 200 200">
      <motion.path
        d="M 50 50 L 150 50 L 150 150 L 50 150 Z M 75 75 L 125 125 M 125 75 L 75 125"
        stroke="#000"
        strokeWidth="4"
        strokeLinecap="round"
        fill="none"
        {...strokeProps}
      />
    </svg>
  )
}
```

### Signature Effect

```tsx
import { motion } from "framer-motion"
import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

export function Signature() {
  const strokeProps = useStrokeDraw({ 
    duration: 2.5,
    ease: [0.4, 0, 0.2, 1],
  })
  
  return (
    <svg width="400" height="100" viewBox="0 0 400 100">
      <motion.path
        d="M 10 50 Q 50 10, 100 50 T 200 50 Q 250 80, 300 50"
        stroke="#1e40af"
        strokeWidth="3"
        strokeLinecap="round"
        fill="none"
        {...strokeProps}
      />
    </svg>
  )
}
```

### Loading Spinner

```tsx
import { motion } from "framer-motion"
import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

export function DrawSpinner() {
  const strokeProps = useStrokeDraw({ 
    duration: 1.5,
    repeat: true,
  })
  
  return (
    <svg width="50" height="50" viewBox="0 0 50 50">
      <motion.circle
        cx="25"
        cy="25"
        r="20"
        stroke="currentColor"
        strokeWidth="3"
        fill="none"
        {...strokeProps}
      />
    </svg>
  )
}
```

### Checkmark Animation

```tsx
import { motion } from "framer-motion"
import { useStrokeDraw } from "@/lib/hooks/use-stroke-draw"

export function CheckmarkDraw() {
  const strokeProps = useStrokeDraw({ 
    duration: 0.5,
    ease: "easeOut",
  })
  
  return (
    <svg width="100" height="100" viewBox="0 0 100 100">
      <motion.path
        d="M 20 50 L 40 70 L 80 30"
        stroke="#10b981"
        strokeWidth="6"
        strokeLinecap="round"
        strokeLinejoin="round"
        fill="none"
        {...strokeProps}
      />
    </svg>
  )
}
```

## Tips

### Use with strokeDasharray

For more control, combine with `strokeDasharray`:

```tsx
<motion.path
  {...strokeProps}
  strokeDasharray="5 5" // Dashed line
/>
```

### Custom Easing

```tsx
const strokeProps = useStrokeDraw({
  ease: [0.6, 0.01, 0.05, 0.95], // Custom cubic bezier
})
```

## Performance

- Uses Framer Motion's optimized path animations
- Hardware accelerated
- Smooth 60fps animation

## Use Cases

- Logo reveals
- Signature animations
- Loading indicators
- Decorative elements
- Icon animations
- Infographic reveals
- Map route animations

<Callout type="warn">
  **SVG Required:** This hook only works with SVG path elements. For other animations, use different hooks.
</Callout>
